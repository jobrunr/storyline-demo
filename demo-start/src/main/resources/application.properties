management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

server.port=8080
spring.application.name=storyline-demo

spring.datasource.url=jdbc:postgresql://localhost:5432/demo
spring.datasource.username=postgres
spring.datasource.password=password

spring.jpa.hibernate.ddl-auto=update

jobrunr.dashboard.enabled=true
jobrunr.background-job-server.enabled=true
jobrunr.background-job-server.worker-count=4

# TODO step 7: payments should have prio over monthly report generation

# TODO step 8a: enterprise customers should have prio for payments
# TODO step 8b: weighted round-robin to avoid starvation on pro customers

# TODO step 9: this server processes national payments only, run a second one for international ones

# TODO step 10: add a rate limiter

# TODO step 11: observability support; add micrometer exposure
