plugins {
    id 'java'
    id 'org.springframework.boot' version '3.5.6'
    id 'io.spring.dependency-management' version '1.1.7'
}

group = 'org.jobrunr'
version = '0.0.1-SNAPSHOT'
description = 'storyline-demo'

compileJava {
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
    options.release = 21
}

compileTestJava {
    sourceCompatibility = JavaVersion.VERSION_21
    targetCompatibility = JavaVersion.VERSION_21
}

repositories {
    mavenCentral()
    maven {
        url "https://repo.jobrunr.io/private-releases/"
        credentials {
            username "$mavenUser"
            password "$mavenPass"
        }
    }
}

dependencies {
    implementation 'org.springframework.boot:spring-boot-starter'               // spring boot
    implementation 'org.springframework.boot:spring-boot-starter-jdbc'          // data source autoconfig
    implementation 'org.jobrunr:jobrunr-pro-spring-boot-3-starter:8.0.5'        // jobrunr pro

    implementation 'com.zaxxer:HikariCP:6.2.1'                                  // postgres + conpooling
    implementation 'org.postgresql:postgresql:42.7.4'

    implementation group: 'com.fasterxml.jackson.core', name: 'jackson-databind', version: '2.18.2'
    implementation group: 'com.fasterxml.jackson.datatype', name: 'jackson-datatype-jsr310', version: '2.18.2'
    implementation group: 'com.fasterxml.jackson.datatype', name: 'jackson-datatype-jdk8', version: '2.18.2'

    testImplementation 'org.springframework.boot:spring-boot-starter-test'
}

test {
    useJUnitPlatform()
}

